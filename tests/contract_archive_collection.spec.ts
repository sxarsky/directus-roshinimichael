// Generated by Skyramp v1.3.13 on 2026-03-18 14:31:03.591661136 +0000 UTC m=+134.143174815
// Command: skyramp generate contract rest http://localhost:8055/archive/test_archive_collection \
// 		--framework playwright \
// 		--language typescript \
// 		--method PATCH \
// 		--output contract_archive_collection.spec.ts \
// 		--output-dir /home/runner/work/directus/directus/tests \
// 		--overwrite \
// 		--provider \
// 		--request-data {"keys":[1],"status_field":"status"} \
// 		--runtime local \
// 		--skipProvisionParents

// Import of required libraries
import { test, expect } from '@playwright/test';
import { SkyrampClient, checkStatusCode, getBaseUrl } from '@skyramp/skyramp';

// URL for test requests
let URL = getBaseUrl("SKYRAMP_TEST_BASE_URL", "http://localhost:8055");

// contract test for /archive/test_archive_collection PATCH
test('testTestArchiveCollectionPatch', async () => {
    // Invocation of Skyramp Client
    const client = new SkyrampClient();
    // Definition of authentication header
    const headers: Record<string, string> = {};
    if (process.env.SKYRAMP_TEST_TOKEN && process.env.SKYRAMP_TEST_TOKEN != "") {
        headers["Authorization"] = "Bearer " + process.env.SKYRAMP_TEST_TOKEN;
    }

    // Request Body
    const testArchiveCollectionPatchRequestBody: string = `{
        "keys": [
            1
        ],
        "status_field": "status"
    }`;

    // Execute Request
    const testArchiveCollectionPatchResponse = await client.sendRequest({
        url: URL,
        path: "/archive/test_archive_collection",
        method: "PATCH",
        body: testArchiveCollectionPatchRequestBody,
        headers: headers
    });

    // Generated Assertions
    expect(checkStatusCode(testArchiveCollectionPatchResponse, "20x"), 'status code').toBeTruthy();
});
